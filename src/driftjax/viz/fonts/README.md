# Bundled fonts

Computer Modern Unicode (CMU) Serif faces — regular (`cmunrm.ttf`), bold
(`cmunbx.ttf`), italic (`cmunti.ttf`) — used by the `presentation` figure
style (`driftjax.viz.style.apply("presentation")`).

License: SI Open Font License 1.1 (free redistribution).
